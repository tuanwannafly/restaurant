# Cấu hình kết nối Oracle Database
# Chỉnh sửa các thông số dưới đây cho phù hợp với môi trường của bạn

db.url=jdbc:oracle:thin:@//localhost:1521/orclpdb
db.username=test
db.password=123
db.driver=oracle.jdbc.OracleDriver

# Connection pool settings
db.pool.size=5
db.timeout=10000
